template<class T>
class node{
	private:
        T value;
        node *next, *prev;
    public:
        node(T val){
            value = val;
            next = prev = NULL;
        }
        // Similarly, all member functions have T in place of the datatype 
};

template<class T>
class linked_list{
	private:
		node *head, *tail;
	public:
	// Member functions with T instead of datatype
};

"home/ashwin/<Directory_With_CMakeLists_txt>"

"cmake home/ashwin/<Directory_With_CMakeLists_txt>"

cmake home/ashwin/cv_assignment_1
